<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="admin">

	<select id="adminMemberManageList" resultType="admin">
	SELECT
		USERNO AS userNo,
   		AUTHORITY_ID AS authorityId,
    	USERID AS userId,
    	USERPWD AS userPwd,
    	USERNAME AS userName,
    	NICK AS nick,
    	EMAIL AS email,
    	ADDRESS AS address,
    	DETAIL AS detail,
    	ENROLLDATE AS enrollDate,
    	CARD_NO AS cardNo,
    	DEL_YN AS delYN
	FROM MEMBER
	</select>
  
  
</mapper>
